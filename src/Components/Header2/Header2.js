import React from 'react';
import styles from './Header.module.css'

const Header2 = () => {
    return (
        <div>
            <h2 className={styles.title}>This is brand new Header</h2>
        </div>
    );
};

export default Header2;